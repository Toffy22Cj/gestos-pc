opencv-python>=4.8.0
mediapipe>=0.10.0
SQLAlchemy>=2.0.0 # Para la base de datos local
scikit-learn>=1.3.0 # Para entrenar clasificador personalizado
pandas>=2.0.0       # Para el manejo del Dataset (CSV)
# Opcionales según el SO:
# pyautogui (Windows)
# xdotool / wmctrl / python-dbus (Linux Genérico)
